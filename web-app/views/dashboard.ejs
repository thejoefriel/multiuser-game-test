<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard — CM 01/02</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: system-ui, sans-serif; background: #1a1a2e; color: #eee; display: flex; justify-content: center; align-items: center; min-height: 100vh; }
    .card { background: #16213e; padding: 2rem; border-radius: 8px; width: 100%; max-width: 500px; }
    h1 { margin-bottom: 1.5rem; text-align: center; }
    .status { background: #0f3460; padding: 1rem; border-radius: 4px; margin-bottom: 1rem; text-align: center; }
    .status.running { border-left: 4px solid #4caf50; }
    .status.stopped { border-left: 4px solid #666; }
    button, .btn { display: inline-block; padding: 0.75rem 1.5rem; background: #e94560; color: #fff; border: none; border-radius: 4px; cursor: pointer; font-size: 1rem; text-decoration: none; text-align: center; }
    button:hover, .btn:hover { background: #c73e54; }
    .btn-stop { background: #666; }
    .btn-stop:hover { background: #555; }
    .actions { display: flex; gap: 1rem; justify-content: center; margin-top: 1rem; }
    .error { background: #e94560; padding: 0.5rem; border-radius: 4px; margin-bottom: 1rem; text-align: center; }
    .meta { font-size: 0.85rem; color: #aaa; margin-top: 0.5rem; }
    nav { display: flex; justify-content: flex-end; margin-bottom: 1rem; }
    nav form button { padding: 0.4rem 0.8rem; font-size: 0.85rem; background: transparent; border: 1px solid #666; }
  </style>
</head>
<body>
  <div class="card">
    <nav>
      <form method="POST" action="/logout">
        <button type="submit">Log out</button>
      </form>
    </nav>
    <h1>Championship Manager 01/02</h1>

    <% if (error === 'start_failed') { %>
      <div class="error">Failed to start game. Please try again.</div>
    <% } %>

    <% if (status.running) { %>
      <div class="status running">
        <p>Your game is <strong>running</strong>.</p>
        <p class="meta">Started: <%= status.startedAt %></p>
      </div>
      <div class="actions">
        <a href="/play" class="btn">Play Now</a>
        <form method="POST" action="/game/stop">
          <button type="submit" class="btn-stop">Stop Game</button>
        </form>
      </div>
    <% } else if (status.pending) { %>
      <div class="status running">
        <p>Your game is <strong>starting up</strong>...</p>
        <p class="meta">This may take a moment.</p>
      </div>
      <div class="actions">
        <a href="/dashboard" class="btn">Refresh</a>
      </div>
    <% } else { %>
      <div class="status stopped">
        <p>No game running.</p>
      </div>
      <div class="actions">
        <form method="POST" action="/game/start">
          <button type="submit">Start Game</button>
        </form>
      </div>
    <% } %>
  </div>
</body>
</html>
